spring:
  application:
    name: ajo-platform

  jpa:
    properties:
      hibernate:
        format_sql: true
    show-sql: true

jwt:
  expiration: 86400000

server:
  port: 8080
  error:
    include-message: always

logging:
  level:
    com.ajo.platform: DEBUG